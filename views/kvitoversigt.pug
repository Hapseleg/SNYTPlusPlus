extends layout.pug
block body
    if(authenticated)
        div(class="container")
            input#hiddenInputForUserAmount(hidden value=users)
            table(class="table")
                thead
                    tr
                        th Emne
                        //TODO add th Kategori???
                        th Dato
                        th Initialer
                        th Mangler at læse
                tbody#snytKvitOversigt